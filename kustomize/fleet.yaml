targetCustomizations:
- name: kuberay-lab
  defaultNamespace: kuberay-operator
  clusterSelector:
    matchLabels:
      cluster: lab
  kustomize:
    dir: .

dependsOn:
  - selector:
    bindle: kuberay-crds
